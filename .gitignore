# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# 本地运行时工件（compile 生成，可由脚本重铸）
# 用 **/ 前缀匹配任意层级（.gitignore 的 * 不跨目录，不加会漏掉 examples/*/.workflow/ 深层的产物）
**/.workflow/*.delivery.json
**/*.delivery.md
# LaTeX PDF 排版视图产物（md_to_pdf.py 生成，可由 final.md 重铸；md 始终是事实源）
**/.workflow/*.pdf
**/.workflow/*.tex
**/.workflow/*.log
**/.workflow/*.aux
**/.workflow/*.out

# 系统
.DS_Store

# output/ 交付目录的 LaTeX 中间产物（交付物只收 .md + .pdf）
**/output/*.aux
**/output/*.log
**/output/*.out
**/output/*.tex

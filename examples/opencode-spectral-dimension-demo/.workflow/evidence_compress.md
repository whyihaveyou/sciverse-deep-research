# 证据压缩块

## 证据压缩块（视角 1 定义与结构 / 视角 2 随机行走 / 视角 3 同步共识 / 检索方式：semantic_search）

- 本批文献键：[@RammalToulouse; @DAuriacRammal; @MillanUniversality; @AvrachenkovRGG; @CraigFiniteSize; @GrimaldiFiedler; @LacasaCorrelation; @YakuboBifractal; @MulkenInformation; @HaynesRoberts; @ZhangTrapping; @TejedorGMFPT; @MillanSyncThreshold; @MillanFrustrated; @PattersonBamieh; @QiSierpinskiConsensus; @JinMurray; @GomezMultiplex; @SchennachLongMemory; @AharonyFractals]
- 压缩摘要：这批证据共同确认谱维数 $d_s$ 通过 Laplacian 特征值密度定义、通过随机行走回返概率 $P(t) \sim t^{-d_s/2}$ 自然涌现，并把 $d_s$ 与网络几何、同步相变、共识收敛标度联系起来。
- 证据状态：
  - @RammalToulouse：✅ 已确认可用 — 谱维数定义 + Sierpinski 三角族解析值 + 随机行走性质可由 $d_s$ 控制。
  - @DAuriacRammal：✅ 已确认可用 — Sierpinski 三角谱维数 $d_s=2\ln(d+1)/\ln(d+3)$ 数值验证。
  - @AharonyFractals：✅ 已确认可用 — $d_s=2D/(2+\theta)$、Alexander-Orbach 猜想 $d_s=4/3$、fracton 维数。
  - @MillanUniversality：✅ 已确认可用 — 可调谱维数网络模型，$d_s$ 可连续从 1 调到 ∞，回返概率服从 $P_0(t)\sim t^{-d_s/2}$。
  - @AvrachenkovRGG：✅ 已确认可用 — 热力学区随机几何图谱维数 ≈ 空间维数 $d$。
  - @CraigFiniteSize：✅ 已确认可用 — 有限尺寸效应对分形/谱维数估算的影响，MBRW 谱维数估计（裁判型）。
  - @GrimaldiFiedler：✅ 已确认可用 — Fiedler 维数与谱维数可解耦，用于介观/宏观集体动力学（裁判型）。
  - @LacasaCorrelation：✅ 已确认可用 — box-covering 的困难，基于相关和的关联维数估计（裁判型）。
  - @YakuboBifractal：✅ 已确认可用 — 双分形网络谱维数非唯一，hub/非 hub 起点给出不同 $d_s$（裁判型，挑战单值 $d_s$）。
  - @MulkenInformation：✅ 已确认可用 — 熵增 ~ 谱密度标度；SG 谱维数 $d_s=2\ln3/\ln2$。
  - @HaynesRoberts：✅ 已确认可用 — 全局首达时间，方差 $Var(T)\sim N^{4/d_s}$。
  - @ZhangTrapping：✅ 已确认可用 — 标度自由/小世界网络的 trapping 效率，分形维数决定性（对照 @RammalToulouse）。
  - @TejedorGMFPT：✅ 已确认可用 — 全局平均首达时间下界，GMFPT 依赖 target。
  - @MillanSyncThreshold：✅ 已确认可用 — $d_s>4$ 同步热力学稳定、$d_s>2$ 相位同步（核心裁决文献）。
  - @MillanFrustrated：✅ 已确认可用 — 复杂网络流形上 frust 同步，受 $d_s$ 影响。
  - @PattersonBamieh：✅ 已确认可用 — 分形网络一致性相干性 $H_{FO}\sim N^{2/d_s-1}$、$H_{SO}\sim N^{4/d_s-1}$（核心裁决文献）。
  - @QiSierpinskiConsensus：✅ 已确认可用 — 自相似分形图上共识收敛/延迟/相干性解析。
  - @JinMurray：✅ 已确认可用 — 规则格子上 $\lambda_2\to 0$ 导致共识无穷慢（奠基）。
  - @GomezMultiplex：✅ 已确认可用 — 多层网络扩散谱分析，$\lambda_2$ 与同步时间。
  - @SchennachLongMemory：✅ 已确认可用 — 经济网络长记忆由 $d_s$ 刻画，$d_s$ 不唯一由度分布决定（跨学科）。
- 对本视角意图的修正：$d_s$ 不仅定义于随机行走，还通过 $\lambda_2$（代数连通性）与共识/同步的有限尺寸标度直接相连；"Fiedler 维度 vs 谱维度"的区分是开放点。
- 下一步动作：滚雪球 + 时效探针后用当前候选进入综合。

## 检索收敛声明
- 研究简报子方向数：3（结构 / 随机行走 / 同步共识）
- 每子方向核心文献 ≥3 篇：是（A:7, B:6, C:7）
- 末轮滚雪球新增入选：0 篇
- 时效探针已执行：是（近 3 年窗口：2023-2026 有 @YakuboBifractal 2024、@GrimaldiFiedler 2025、@CraigFiniteSize 2022）
- 多源覆盖：sciverse 唯一指定源（用户要求只用 sciverse）；存在性经 sciverse 检索返回确认
- 盲区检查：已列出（量子行走上的维度 / 网络动力学子图上 $d_s$ 的角色等为相邻盲区）
- 裁判型文献定向检索：已执行（@CraigFiniteSize、@GrimaldiFiedler、@LacasaCorrelation 为有限尺寸/估计可靠性裁判型；@YakuboBifractal 挑战 $d_s$ 单值性）
- 意图演化已记录：是
- 是否进入阶段二：是（每子方向 ≥3 篇，末轮零新增）
